spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/courseimooc?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 1375632510

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.cy.server.domain

logging:
  level:
    com.cy.server.mapper: trace
